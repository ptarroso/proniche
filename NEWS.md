# proniche 0.5

-   bundled example data

-   improved documentation examples

-   added informative error message for convexhull

-   added CITATION file


# proniche 0.4

-   correctly overlay bounds in domain plot

-   fixed nvars in print method

-   add Formoso-Freire et al. ref for quantReclass

-   model->object/x to fix S3 generic/method consistency warning

-   added documentation for plot and predict methods

-   declare imports; some more documentation fixes


# proniche 0.3

-   renamed function `model()` to `promodel()`

-   added arguments `na.rm`, `dup.rm` and `verbosity` to `promodel()`

-   added function `quantReclass()`


# proniche 0.2

-   implemented for data frame (not just SpatRaster) variables

-   added some more checks to prevent errors, e.g. due to wrong number of variables for input 'method'


# proniche 0.1

-   added package files (DESCRIPTION, NAMESPACE, NEWS; 'man' and 'R' folders)

-   added `model()` (so far, the only exported function; wrapper for 'functions.R') and its documentation / help file

